# Jennifer Forsyth

## Check Your Understanding
1. I would fit my automated tests within a Github action that runs whenever the code is pushed. This is to catch any issues or bugs before they get merged into the main codebase. Having them merged would create even more issues. Github actions could also help ensure that the code works as well.
2. No.
3. The difference between navigation and snapshot is that navigation mode will analyze a page right after it loads & will give a performance metric, meanwhile in snapshot mode, it analyzes a page in its current state.
4. The document does not have a meta description so add a meta description, <html> does not have a [lang] attribute so add a lang attribute to specify the page language, and consider reducing the time spent parsing, executing, and compiling JS by delivering smaller JS payloads like removing unused code.










